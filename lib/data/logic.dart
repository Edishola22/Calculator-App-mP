class Logic{
  ///haven't implemented this section yet will do later and update
  double addNumbers(btnTxt, secondTxt ) {
    var num = int.parse(btnTxt);
    var secNum = double.parse(secondTxt);
    var result = num + secNum;

    return  result;
  }
  // double performCal(btnTxt, function, secondTxt ) {
  //   var num = double.parse(btnTxt);
  //   var secNum = double.parse(secondTxt);
  //   var result = num + function + secNum;
  //   return result;
  // }

  void showResult(btnTxt) {

  }
}